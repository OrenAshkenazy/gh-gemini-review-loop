capability: secret_wiring
inputs:
  secret_name: required
  env_var: required
  service: required
generates:
  - external_secret
  - helm_env_wiring
checks:
  - helm_template
  - policy
  - naming_convention
approval:
  required_from:
    - platform-secrets
human_gate: secret value provisioning
